import React from "react";
import GameBrowser from "../../components/gameslist/GameBrowser";

export default function Gamelist() {
    return (
        <React.Fragment>
            <GameBrowser />
        </React.Fragment>
    );
}